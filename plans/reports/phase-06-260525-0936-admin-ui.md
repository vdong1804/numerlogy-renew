# Phase 06 Implementation Report — Admin UI Next.js

**Status:** completed  
**Date:** 2026-05-25

---

## Files Modified

### New lib files (3)
- `src/lib/admin-api.ts` — fetch wrapper, Bearer token, 401 guard, helpers: getJson/postJson/putJson/patchJson/del/uploadFile
- `src/lib/admin-auth.ts` — useAdminAuth() hook: /auth/me check, is_superuser guard, logout
- `src/lib/content-resources.ts` — 23-slug registry with Vietnamese labels, hasExtraContent/isPhoneMaster flags

### New components (8)
- `src/components/admin/admin-layout.tsx` (173 lines) — sidebar nav grouped by section + topbar + auth guard
- `src/components/admin/rich-text-editor.tsx` (152 lines) — TipTap: StarterKit+Image+Link+Table, image upload, source-view toggle
- `src/components/admin/content-table.tsx` (197 lines) — TanStack Table generic: search + pagination + rows
- `src/components/admin/content-form.tsx` (126 lines) — react-hook-form+Zod, main-number extra fields, phone-master-data variant
- `src/components/admin/content-form-fields.tsx` (108 lines) — sub-components: BaseFields, RichField, PhoneMasterFields
- `src/components/admin/generic-crud-form.tsx` (121 lines) — reusable form for news/packages/banks
- `src/components/admin/confirm-dialog.tsx` (89 lines) — modal confirm with danger variant
- `src/components/admin/payment-approval-card.tsx` (164 lines) — payment card with approve/reject + ConfirmDialog

### New pages (18)
```
src/pages/admin/
├── index.tsx                          # dashboard: stat cards + resource grid
├── login.tsx                          # email+pw login, is_superuser check
├── content/[resource]/
│   ├── index.tsx                      # list: paginated table + search + delete
│   ├── new.tsx                        # create form
│   └── [id].tsx                       # edit form + delete
├── users/
│   ├── index.tsx                      # paginated user list
│   └── [id].tsx                       # user detail
├── news/
│   ├── index.tsx                      # news list
│   ├── new.tsx                        # create news
│   └── [id].tsx                       # edit/delete news
├── packages/
│   ├── index.tsx                      # packages list
│   ├── new.tsx                        # create package
│   └── [id].tsx                       # edit/delete package
├── banks/
│   ├── index.tsx                      # banks list
│   ├── new.tsx                        # create bank
│   └── [id].tsx                       # edit/delete bank
└── payments/
    ├── index.tsx                      # pending/all payments + status filter
    └── [id].tsx                       # payment detail + approve/reject
```

### Modified files (2)
- `package.json` — added 13 new dependencies (TipTap suite, TanStack Table, Zod, DOMPurify)
- `.env.example` — added `NEXT_PUBLIC_API_BASE=http://localhost:8000`

---

## Dependency Additions

```json
"@tiptap/react": "^2.6.0",
"@tiptap/starter-kit": "^2.6.0",
"@tiptap/extension-image": "^2.6.0",
"@tiptap/extension-link": "^2.6.0",
"@tiptap/extension-table": "^2.6.0",
"@tiptap/extension-table-row": "^2.6.0",
"@tiptap/extension-table-cell": "^2.6.0",
"@tiptap/extension-table-header": "^2.6.0",
"@tanstack/react-table": "^8.20.0",
"zod": "^3.23.0",
"dompurify": "^3.1.0",
"@types/dompurify": "^3.0.5"
```

Note: `react-hook-form` (7.43.9) and `@hookform/resolvers` (3.0.1) already in package.json — NOT duplicated.

---

## TypeScript Check Status

- **Cannot find module** errors: all from uninstalled packages (@tiptap/*, @tanstack/react-table, zod) — expected, resolve after `npm install`
- **Pre-existing errors** (not our code): `BankInfo.tsx` unused var, `TextCopy.tsx` unused var — untouched
- **Our code errors**: 0 after fixes (corrected: error.message rendering, implicit any in cell renderers, unused import, optional chaining)

---

## Install Instructions

Run in `Numerology-Landing-Page/`:
```bash
npm install
```

---

## Deviations from Plan

- `@tiptap/extension-table-row/cell/header` imported as separate packages (plan listed them) — correct
- DOMPurify added to deps but not inline-used in list views yet (HTML preview sanitization via `dangerouslySetInnerHTML` + DOMPurify can be added in `BoxContentDetail` if needed — YAGNI for now, package is available)
- `image-upload-button.tsx` not created as separate file — functionality merged into `rich-text-editor.tsx` toolbar (DRY, plan shows it as optional)
- Zod already existed in plan's dep list; `yup` was in existing deps — using Zod for admin forms as specified

---

## Ready: Yes

- 18 pages, 8 components, 3 lib files, 2 files updated
- npm install required: **YES**
- All files ≤200 lines
- Vietnamese UI labels throughout
- Bearer token in localStorage (`admin_access_token`)
- No existing pages touched
